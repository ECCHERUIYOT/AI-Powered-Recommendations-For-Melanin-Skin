streamlit==1.24.0  # for building the web app
pandas==2.1.1      # for data manipulation
numpy==1.26.0      # for numerical operations
ast==0.1.0         # for abstract syntax tree parsing (might be installed by default)
pickle5==0.0.11    # for model loading (standard pickle may already be included)
scikit-learn==1.3.1  # for machine learning models
